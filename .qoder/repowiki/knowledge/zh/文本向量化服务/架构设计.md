- 采用 FastAPI 构建 RESTful API，暴露 `/embed`（单条）和 `/batch_embed`（批量）端点。
- 核心推理逻辑封装在 `Embedder` 类中，执行 Tokenizer 编码、ONNX 模型推理、Mean Pooling 及 L2 归一化流程。
- 支持 Mock 模式（通过环境变量或参数触发），在未下载模型时生成确定性随机向量以验证接口连通性。
- 依赖管理使用 `uv` 工具链，模型文件通过 `huggingface_hub` 脚本动态下载至本地 `models/` 目录。
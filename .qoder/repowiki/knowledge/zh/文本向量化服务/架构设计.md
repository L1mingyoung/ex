- 入口层：`main.py` 定义 FastAPI 应用，暴露 `/embed`、`/batch_embed` 及健康检查接口。
- 核心逻辑层：`embedder.py` 封装 `Embedder` 类，负责通过 Tokenizer 进行文本预处理，并利用 ONNX Runtime 执行模型推理及均值池化（Mean Pooling）归一化。
- 资源管理层：`scripts/download_model.py` 负责从 Hugging Face Hub 下载并本地化管理 Jina 中文嵌入模型及分词器文件。
- 依赖方向：API 层依赖核心逻辑层，核心逻辑层依赖本地模型文件与环境配置。
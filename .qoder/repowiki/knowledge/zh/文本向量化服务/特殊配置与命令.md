- 模型初始化：首次运行需执行 `python scripts/download_model.py` 下载 ONNX 模型与分词器至 `models/` 目录。
- Mock 模式：在模型未就绪时，可通过 `MOCK_EMBEDDING=1` 环境变量或 `--mock` 参数启动服务，使用基于哈希的随机向量模拟响应以验证流程。
- 启动命令：`uv run uvicorn main:app --port 8000`。
- 入口层：`main.ts` 作为应用引导入口，负责创建 Nest 实例并配置 CORS 策略。
- 核心装配：`app.module.ts` 作为根模块，集成 TypeORM（PostgreSQL + pgvector）、ConfigModule 以及 ServeStaticModule，并统一导入 Characters、Sessions、Chat 等业务子模块。
- 基础服务：提供默认的 `AppController` 和 `AppService` 用于健康检查或初始状态验证。
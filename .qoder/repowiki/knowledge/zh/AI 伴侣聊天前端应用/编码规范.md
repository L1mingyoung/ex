- API 层采用纯 TypeScript 实现，不依赖 DOM 或 React API，以确保在小程序等非浏览器环境中的可移植性。
- 全局状态操作通过 Context 提供的封装函数（如 `createCharacter`）触发，组件内禁止直接 dispatch action，保持业务逻辑集中。
- 流式响应处理统一使用 `AbortController` 进行管理，支持用户手动中断 AI 生成过程。
- 组件样式采用同目录下的 `.css` 文件进行局部作用域管理，并通过 `id` 或语义化类名进行布局控制。